var inc = 0;
var start = 0;

function setup(){
    createCanvas(800, 800,);
    background(80);
}

function draw(){
    loadPixels();
    background(80);
    stroke(255);
    noFill();

    beginShape();
    for (var x = 0; x < width; x++) {
        
    }
    endShape();

    start += inc;
}